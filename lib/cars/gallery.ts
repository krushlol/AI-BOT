/**
 * Photo gallery for each car. Keys match car IDs in data.ts.
 * IMPORTANT: Only add images you have confirmed show the correct car.
 * Unverified images were removed — each car defaults to its single verified main photo.
 */
export const carGalleries: Record<string, string[]> = {}
